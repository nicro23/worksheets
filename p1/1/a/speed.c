#include <iostream.h>
void main()
{
    cin >> km;
    cin >> hr;
    kilomin = km/(hr*60);
    meterhr = (km*1000)/hr;
    metermin = (km*1000)/(hr*60);
    cout << kilomin;
    cout << meterhr;
    cout << metermin;
}
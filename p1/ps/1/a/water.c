#include <iostream.h>
void main()
{
    cin >> applew8r;
    cin >> avow8r;
    cin >> wtrwtr;
    cin >> aw;
    cin >> avw;
    cin >> ww;
    water = aw * (applew9r/100) + avw * (avow8r/100) + ww * (wtrwtr/100);
    cout << water;
}
#include <iostream.h>
void main()
{
    cin >> capacity
    cin >> a1w
    cin >> a2w
    cin >> a3w
    cin >> k1w
    cin >> k2w
    cin >> k3w

    usage = (a1w + a2w + a3w + k1w + k2w + k3w)/capacity * 100

}